package game;

import java.util.HashMap;
import java.util.Map;

import game.enumeration.GameStates;
import game.enumeration.PlayerNum;

public class GameState {

	/* public void update() {
		switch (currentState) {
			
			case PLAYING:
				updateMainGame();
				if (PACMAN_DEATH) {
					currentState = PACMAN_DEATH;
				}
			break;
		
			case GAME_OVER:
				updateGameOver();
				currentState = GAME_OVER;
			break;
		
			case PACMAN_DEATH:
				PACMAN_LIVES -= 1;
				if (PACMAN_LIVES != 0) {
				restartMainGame();
				} else {
					currentState = GAME_OVER;
				}
			break;
		}
	} */

}

